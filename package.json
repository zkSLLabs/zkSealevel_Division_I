{
  "name": "zksl-root",
  "private": true,
  "scripts": {
    "kat:ds": "node scripts/kats/ds_kat.js",
    "kats:all": "node scripts/kats/ds_kat.js && node scripts/kats/ds_negative_kat.js && node scripts/kats/anchor_proof_kat.js",
    "build:services": "npm --prefix orchestrator ci && npm --prefix orchestrator run build && npm --prefix indexer ci && npm --prefix indexer run build",
    "test:orchestrator": "npm --prefix orchestrator run test",
    "test:kats": "npm run kats:all",
    "test:all": "npm run test:orchestrator && npm run test:kats",
    "conformance": "node scripts/conformance.js"
  },
  "dependencies": {
    "blake3": "^2.1.9"
  },
  "engines": { "node": ">=20 <21" }
}


